
```sh
$ curl -sf https://raw.githubusercontent.com/kanorix/dotfiles/experimental/setup.sh | sh -s
```

## linux

```sh
$ apt update
$ apt install -y curl
$ curl -sf https://raw.githubusercontent.com/kanorix/dotfiles/experimental/setup.sh | sh -s
```
